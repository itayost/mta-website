{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "echo \"$CLAUDE_FILE_PATH\" | grep -qE '(package-lock\\.json|yarn\\.lock|\\.env)' && echo 'BLOCK: This file should not be edited directly' && exit 1 || true"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "file=\"$CLAUDE_FILE_PATH\"; [[ \"$file\" == *.ts || \"$file\" == *.tsx ]] && npx next lint --fix --file \"$file\" 2>/dev/null || true"
      }
    ],
    "Notification": [
      {
        "matcher": "permission_prompt",
        "hooks": [{ "type": "command", "command": "afplay /System/Library/Sounds/Ping.aiff" }]
      },
      {
        "matcher": "idle_prompt",
        "hooks": [{ "type": "command", "command": "osascript -e 'display notification \"Claude is waiting\" with title \"Claude Code\"'" }]
      }
    ]
  }
}
